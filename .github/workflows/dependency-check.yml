name: OWASP Dependency-Check - Backend

on:
  push:
    branches: [ main ]

jobs:
  dependency-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Ejecutar OWASP Dependency-Check via Maven
        run: |
          mvn org.owasp:dependency-check-maven:check \
            -DfailBuildOnCVSS=7 \
            -Dformat=HTML \
            -DoutputDirectory=reports \
            -DnvdApiKey=${{ secrets.NVD_API_KEY }}
        continue-on-error: true

      - name: Buscar reporte generado
        run: find . -name "dependency-check-report.html" 2>/dev/null

      - name: Subir reporte
        uses: actions/upload-artifact@v4
        with:
          name: reporte-backend
          path: target/dependency-check-maven/